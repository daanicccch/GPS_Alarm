<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GPSclocker.Views.GPSitems"
             Title="{Binding Title}"
             xmlns:local="clr-namespace:GPSclocker.ViewModels"  
             xmlns:model="clr-namespace:GPSclocker.Models"
             x:Name="BrowseItemsPage"
             BackgroundColor="#e6e1f5">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Command="{Binding AddItemCommand}" />
    </ContentPage.ToolbarItems>
    <!--
  x:DataType enables compiled bindings for better performance and compile time validation of binding expressions.
  https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/data-binding/compiled-bindings
-->
    <RefreshView x:DataType="local:GpsItemsViewModel" Command="{Binding LoadItemsCommand}" IsRefreshing="{Binding IsBusy, Mode=TwoWay}">
        <CollectionView  x:Name="GpsItemsListView"
            x:DataType="local:GpsItemsViewModel"
            ItemsSource="{Binding GpsItems}"
            SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10" x:DataType="model:GpsItem">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="60" />
                            </Grid.ColumnDefinitions>
                            <Label Text="{Binding Adress}"
                            LineBreakMode="NoWrap"
                            Style="{DynamicResource GpsListItemTextStyle}"
                            Grid.Column="0"                         
                             />
                            <Switch x:Name="switchAlarm" Grid.Column="1" WidthRequest="60" HeightRequest="30" Toggled="switchAlarm_Toggled" IsToggled="{Binding IsEnabled}"/>
                        </Grid>
                        <Label Text="{Binding Description}" 
                         LineBreakMode="NoWrap"
                         Style="{DynamicResource ListItemDetailTextStyle}"
                          />
                        <StackLayout.GestureRecognizers>
                            <TapGestureRecognizer 
                            NumberOfTapsRequired="1"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type local:GpsItemsViewModel}}, Path=ItemTapped}"		
                            CommandParameter="{Binding .}">
                            </TapGestureRecognizer>
                        </StackLayout.GestureRecognizers>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </RefreshView>
</ContentPage>